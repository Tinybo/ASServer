let config = {
    database: 'attendance_system',    // 数据库名
    username: 'root',       // 用户名
    password: '221506',     // 密码
    host: 'localhost',      // 主机名
    port: 3306              // 端口号
}

module.exports = config;